# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Histoire de Poche' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Histoire de Poche

  #tag list
  pod 'TagListView'

  #better rest calls
  pod 'Alamofire', '~> 4.7'

  #caching of images data
  pod 'Kingfisher', '~> 4.0'

  pod 'SCLAlertView'
  
end
